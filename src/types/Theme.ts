type Theme = {
    color: {
        primary: string,
        secondary: string,
        background: string,
        "on-background": string,
        "on-background-dark": string,
        "on-primary": string,
    }
}

export default Theme